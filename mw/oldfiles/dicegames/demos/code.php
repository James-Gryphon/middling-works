
<?php
/*
if(isset($_GET['dc']
if(is_numeric($_GET['dc']) && $_GET['dc'] > 0 && $_GET['dc'] < 100){
$dc = $_GET['dc']; } else { $dc = 1;}

if(is_numeric($_GET['sc']) && $_GET['sc'] > 0 && $_GET['sc'] < 100){
$sc = $_GET['sides']; } else { $sc = 4;}

if(is_numeric($_GET['ad'])){
$ad = $_GET['ad']; } else { $ad = 0;}

if(is_numeric($_GET['md']) && $_GET['md'] > 0 && $_GET['md'] < 100){
$md = $_GET['md']; } else { $md = 1;}

if($_GET['rule'] == “high” || $_GET['rule'] == “low”){
$rule = $_GET['rule']; } else { $_GET['rule'] = “high”;}

if(is_numeric($_GET['sr'])){
$sr = $_GET['sr']; } else { $sr = 0;}

if(is_numeric($_GET['sl'])){
$sl = $_GET['sl']; } else { $sl = 0;}

if(isset($_GET['name']){ 
$name = htmlspecialchars($_GET['name']);
}
else { $name = Unnamed Entrant();}

if(isset($_GET['col']){
$color = $_GET['col'];
// Cut the string off to the point where it's valid; if we expand the hex code to 6-digits, we should do this in reverse
if(strlen($color) > 3)){
$color = truncate($color, 3);
}
// Make sure there's only hex-code symbols in the string  - thanks to Kobi for the delimiter tip - "PHP regex strings need delimiters."
$var = preg_match("/^([0-9a-fA-f]*)\Z/", $var);
if (!var){ $color = 777;}
else { $color = 777;}

*/

?>